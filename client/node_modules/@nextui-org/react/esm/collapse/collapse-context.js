import React from 'react';
const defaultContext = {
  values: []
};
export const CollapseContext = /*#__PURE__*/React.createContext(defaultContext);
export const useCollapseContext = () => React.useContext(CollapseContext);