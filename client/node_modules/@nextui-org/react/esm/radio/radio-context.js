import React from 'react';
const defaultContext = {
  color: 'primary',
  textColor: 'default',
  disabledAll: false,
  inGroup: false
};
export const RadioContext = /*#__PURE__*/React.createContext(defaultContext);
export const useRadioContext = () => React.useContext(RadioContext);