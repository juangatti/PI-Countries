import { NormalSizes } from '../utils/prop-types';
declare type PaginationSize = {
    font: string;
    width: string;
};
export declare const getPaginationSizes: (size: NormalSizes | number) => PaginationSize;
export {};
