import React from 'react';
const defaultContext = {
  color: 'primary',
  textColor: 'default',
  disabledAll: false,
  inGroup: false,
  values: []
};
export const CheckboxContext = /*#__PURE__*/React.createContext(defaultContext);
export const useCheckbox = () => React.useContext(CheckboxContext);