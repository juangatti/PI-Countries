import { CSSProperties } from 'react';
import { NormalSizes } from '../utils/prop-types';
export declare const getCheckboxSize: (size: NormalSizes | number) => string;
export declare const getIconCheckStyle: (size: NormalSizes | number, indeterminate?: boolean | undefined) => CSSProperties;
