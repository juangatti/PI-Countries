"use strict";

exports.__esModule = true;
exports.getPaginationSizes = void 0;

const getPaginationSizes = size => {
  const sizes = {
    mini: {
      font: '10px',
      width: '24px'
    },
    small: {
      font: '12px',
      width: '28px'
    },
    medium: {
      font: '14px',
      width: '34px'
    },
    large: {
      font: '16px',
      width: '40px'
    },
    xlarge: {
      font: '18px',
      width: '46px'
    }
  };
  if (typeof size === 'number') return {
    font: `calc(${size}px / 2.55)`,
    width: `${size}px`
  };
  return sizes[size];
};

exports.getPaginationSizes = getPaginationSizes;