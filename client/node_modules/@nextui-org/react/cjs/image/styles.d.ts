import { NextUIThemesPalette } from '../theme';
export declare type BrowserColors = {
    color: string;
    barBgColor: string;
    inputBgColor: string;
    borderColor: string;
    titleColor: string;
};
export declare const getBrowserColors: (invert: boolean, palette: NextUIThemesPalette) => BrowserColors;
