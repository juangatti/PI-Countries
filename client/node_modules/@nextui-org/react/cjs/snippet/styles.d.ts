import { SnippetTypes } from '../utils/prop-types';
import { NextUIThemesPalette } from '../theme';
export declare type SnippetStyles = {
    color: string;
    border: string;
    bgColor: string;
};
export declare const getStyles: (type: SnippetTypes, palette: NextUIThemesPalette, fill?: boolean | undefined) => SnippetStyles;
