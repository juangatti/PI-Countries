import { SimpleColors, TooltipColors } from '../utils/prop-types';
import { NextUIThemesPalette } from '../theme';
export declare type TooltipContentColors = {
    bgColor: string;
    color: string;
};
export declare const getColors: (color: TooltipColors | string, textColor: SimpleColors | string, palette: NextUIThemesPalette) => TooltipContentColors;
