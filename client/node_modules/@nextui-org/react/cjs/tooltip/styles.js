"use strict";

exports.__esModule = true;
exports.getColors = void 0;

var _color = require("./../utils/color");

const getColors = (color, textColor, palette) => {
  let bgColor = (0, _color.getNormalColor)(color, palette, palette.background);
  bgColor = bgColor.includes('linear-gradient') ? (0, _color.hexFromString)(bgColor, palette.text, true) : bgColor;
  const labelColor = textColor && textColor !== 'default' ? (0, _color.getNormalColor)(textColor, palette) : (0, _color.invertHex)(bgColor);
  return {
    color: labelColor,
    bgColor
  };
};

exports.getColors = getColors;